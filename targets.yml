# List of all supported target types
docker_image: ecohub/discover_target:v1.2
targets:
  - type: aci
    label: ACI
    icon: aci.png
    parameters:
      - name: aci_hostname
        label: APIC hostname or IP
        icon: dns
        type: text
        required: true
      - name: aci_username
        label: Username
        icon: person
        type: text
        required: true
      - name: aci_password
        label: Password
        icon: lock
        type: secret
        required: true
  - type: tetration
    label: Tetration
    icon: tetration.png
    parameters:
      - name: tetration_hostname
        label: Cluster hostname or IP
        icon: dns
        type: text
        required: true
      - name: tetration_api_key
        label: API Key
        icon: vpn_key
        type: text
        required: true
      - name: tetration_api_secret
        label: API Secret
        icon: lock
        type: secret
        required: true
  - type: awx
    label: AWX
    icon: awx.png
    parameters:
      - name: awx_hostname
        label: AWX hostname or IP
        icon: dns
        type: text
        required: true
      - name: awx_token
        label: API Key
        icon: lock
        type: secret
        required: true
